server:
  port: 8000

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/spr_test
    username: root
    password: 123456
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
    generate-ddl: true